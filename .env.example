# Environment (development or production)
FLASK_ENV=production

# The port the API will listen on
PORT=5000

# CORS Allowed Origin (use * for loose, or specific origins like http://localhost:3000 for strict)
ALLOWED_ORIGIN=*
